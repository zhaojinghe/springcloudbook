spring: 
  application:
    name: application-dev
  cloud:
    config:
      name: crm
      uri: http://configserver.school.com:4001  # 配置configserver地址
      profile: test  # 级别
      label: master  # 分支 git中 默认master
      fail-fast: true