package com.school.common.service.impl;

import org.apache.log4j.Logger;

/**
 * ServiceImpl基类
 */
public abstract class BaseServiceImpl {
	/** 记录日志 */
    protected final Logger logger = Logger.getLogger(getClass());
}
